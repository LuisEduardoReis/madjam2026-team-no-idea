
export type PackedTexturesData = {
    frames: {
        filename: string;
        frame: {
            x: number,
            y: number,
            w: number,
            h: number,
        }
    }[];
};
